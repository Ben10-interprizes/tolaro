services:
  - type: web
    name: appsmith-backend
    env: docker
    dockerfilePath: backend/Dockerfile   # Path to Dockerfile, relative to repo root
    buildCommand: ""
    startCommand: ""
    branch: main
    autoDeploy: true
    # Add this line:
    buildContext: backend                # <-- Add this
